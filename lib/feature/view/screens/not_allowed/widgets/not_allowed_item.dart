import 'package:flutter/material.dart';

class NotAllowedItem extends StatelessWidget {
  const NotAllowedItem({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}
import 'package:flutter/material.dart';

import '../../../../../util/app_shaerd_data.dart';

class StoreItemDetails extends StatelessWidget {
  const StoreItemDetails({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    screenUtil(context);
    return Scaffold(
      
    );
  }
}